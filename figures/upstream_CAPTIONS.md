# Figure captions

One line per shipped figure, naming the file, plus any
in-figure title suppressed for publication.

- **08_study2_gromacs_screening** — suppressed title: (A) Which GROMACS parameter changes wallclock the most — and which of those changes is distinguishable from chance
- **08_study2_gromacs_screening** — suppressed title: 27 configs ranked by production wallclock
- **08_study2_gromacs_screening** — suppressed title: 27 configs ranked by usable wallclock (penalised by MD success rate)
- **08_study2_gromacs_screening** — suppressed title: BEDROC (α=20) per (target, config) — PARTIAL (2465/3043 rescores)
- **08_study2_gromacs_screening** — suppressed title: Do the configs really differ on early enrichment?
- **08_study2_gromacs_screening** — suppressed title: GBSA rescores completed per (config, target) — 30 = full
- **08_study2_gromacs_screening** — suppressed title: Mean wallclock per target (aggregated over 27 configs)
- **08_study2_gromacs_screening** — suppressed title: Study 2 — GROMACS parameter influence on production wallclock
- `02_gbsa_vs_docking_fig1.png` — **02_gbsa_vs_docking** — Per-target ranking quality — GBSA (navy) vs docking (gold) on Kendall τ, BEDROC α=20 and ROC-AUC, in one shared target order across all three panels. Dashed step = that target's own permutation null, 95th percentile (20 000 draws). GBSA beats docking on 7/8 targets on BEDROC and clears chance on 1/8.
- `03_selection_correction_fig1.png` — **03_selection_correction** — Distribution of the one-sided panel Wilcoxon p over all 48 physics combos. Gold = the selected best-of-48 (p = 0.027, winner's curse); navy dash-dot = the selection-corrected max-T p (0.113, exact, 29/256 sign vectors); grey dashed = α = 0.05. The corrected value does not clear the threshold.
- `04_docking_baseline_fig1.png` — **04_docking_baseline** — Whole-list ROC-AUC per target, ordered by docking AUC. Bars = docking (gold where anti-predictive, i.e. below 0.5); diamonds = GBSA on the same axis. Docking is anti-predictive on 5/8 targets, so the notebook-02 BEDROC edge is docking failing at least as much as GBSA succeeding.
- `05_physics_importance_fig1.png` — **05_physics_importance** — η² of the four MM-GBSA physics parameters on target-blocked BEDROC (navy) and pooled Kendall τ (gold). Log x-axis, because the eight values span five orders of magnitude; every bar carries its value. Only intdiel exceeds the 5 % line, and only on the blocked BEDROC response.
- `05_physics_importance_fig2.png` — **05_physics_importance** — Two heat maps over the full 48 combos x 9 targets grid — (A) Kendall tau on a diverging scale centred at 0, (B) BEDROC on a sequential scale — with the locked combo's row outlined. Target explains far more variance than combo does on both metrics, which is the notebook's point: the parameter response is target-specific, not uniform.
- `06_timestep_throughput_fig1.png` — **06_timestep_throughput** — η² of the five production parameters on nominal ns/day versus on real compute (steps/sec), from the controlled single-GPU L40S smoke. dt dominates ns/day mechanically (a larger timestep simulates more nanoseconds per step); on steps/sec the lever is MTS, not dt.
- `07_timestep_stability_fig1.png` — **07_timestep_stability** — Production fail-rate by timestep (left) and usable ns/GPU-day after crashes (right). 2 fs is stable at 0 % failure; 4 fs with MRF = 3.0 fails 84 % (LINCS); 3 fs gives the best usable throughput at 1012 ns/GPU-day once failures are charged at their measured cost.
- `doe/09_doe_analysis_s1_effects.png` — **09_doe_analysis** — NIST DOE step 6 for Study 1: estimated |effects| with their permutation nulls. Raw and target-blocked bars are identical here because the factorial is fully crossed, so at the combo unit blocking is a no-op. intdiel and igb sit at the permutation floor.
- `doe/09_doe_analysis_s1_interactions.png` — **09_doe_analysis** — NIST DOE step 4 for Study 1: the lower-triangle interaction matrix over the four physics parameters.
- `doe/09_doe_analysis_s1_mean_block.png` — **09_doe_analysis** — NIST DOE steps 3 and 5 for Study 1: main-effect means per parameter (top) and the block plot with one grey line per target (bottom), which shows whether an effect holds within every target.
- `doe/09_doe_analysis_s1_ordered_scatter.png` — **09_doe_analysis** — NIST DOE steps 1–2 for Study 1 (the 48-cell physics factorial): ordered data plot and DOE scatter of BEDROC against each of the four parameters.
- `doe/09_doe_analysis_s2_bedroc_mean_block.png` — **09_doe_analysis** — The same DOE mean and block plots on the BEDROC response, provisional (n≥8 gate, 99 of 243 cells). Note dt = 4 exists on one target only.
- `doe/09_doe_analysis_s2_effects.png` — **09_doe_analysis** — NIST DOE step 6 for Study 2 on wallclock: |effects| against the config-unit, dt-stratified permutation null. dt is blocked out by its own block and gap/nstlist are marked not estimable, because within each dt stratum their partitions coincide; only MTS has both a bar and a null it clears.
- `doe/09_doe_analysis_s2_interactions.png` — **09_doe_analysis** — NIST DOE step 4 for Study 2. Panels involving gap × nstlist are not estimable in this resolution-III array conditional on dt.
- `doe/09_doe_analysis_s2_mean_block.png` — **09_doe_analysis** — NIST DOE steps 3 and 5 for Study 2 on wallclock: main-effect means and the per-target block plot.
- `doe/09_doe_analysis_s2_ordered_scatter.png` — **09_doe_analysis** — NIST DOE steps 1–2 for Study 2 (the Taguchi L27 production screen): ordered data plot and DOE scatter of wallclock against each of the five parameters.
- `study2/08_study2_gromacs_screening_bedroc_control.png` — **08_study2_gromacs_screening** — The common-set control: BEDROC per config on the ligand set held fixed across configs. It reshuffles the naive ordering (leader sp08 -> sp04), which is why no config is selected.
- `study2/08_study2_gromacs_screening_bedroc_partial.png` — **08_study2_gromacs_screening** — Per-config BEDROC computed on partially covered cells, labelled PARTIAL. Kept separate from the common-set analysis because configs scored on different ligand subsets are not comparable.
- `study2/08_study2_gromacs_screening_config_ranking.png` — **08_study2_gromacs_screening** — The 27 L27 configs ranked by mean production wallclock, with the three dt families visible as separated bands.
- `study2/08_study2_gromacs_screening_gbsa_coverage.png` — **08_study2_gromacs_screening** — GBSA rescores completed per (config, target) cell, out of 30 ligands; marks where the campaign's missing 18.1 % sits and which cells pass the n>=8 gate.
- `study2/08_study2_gromacs_screening_param_effects.png` — **08_study2_gromacs_screening** — MAIN DELIVERABLE. (A) Tornado of the wallclock range across each production parameter's levels, with the 95th percentile of the config-unit, dt-stratified null marked in gold; navy = clears its null, grey = does not. (B) Level means per parameter. Only dt and MTS clear.
- `study2/08_study2_gromacs_screening_pareto_bedroc.png` — **08_study2_gromacs_screening** — Mean BEDROC against usable speedup versus sp00 (stability-weighted) per config — what a ranking gain would cost in compute, if any config were distinguishable. Labelled 'the naive view' because the common-set control below reorders it.
- `study2/08_study2_gromacs_screening_reproducibility_floor.png` — **08_study2_gromacs_screening** — Per-config BEDROC spread within the dt = 2 family, sorted by mean — the study's measured minimum meaningful difference (median |dBEDROC| 0.065, q95 0.615). NOT a replicate: those configs differ in rcoulomb, gap and MTS.
- `study2/08_study2_gromacs_screening_usable_wall_ranking.png` — **08_study2_gromacs_screening** — The same 27 configs ranked by stability-weighted wallclock (seconds per *usable* 20 ns). Penalising by MD success rate moves the fastest raw config, sp25 (dt = 4, MTS = 3), to slowest.
- `study2/08_study2_gromacs_screening_wallclock_by_target.png` — **08_study2_gromacs_screening** — Mean wallclock per target, aggregated over all 27 configs, showing that between-target variation dwarfs between-config variation — the reason every effect in notebook 09 is blocked on target.
- `temporal/10_temporal_convergence_trajectory_length.png` — **10_temporal_convergence** — (A) BEDROC against cumulative trajectory length on a log axis, every target in grey and the panel median in navy, with the measured between-config spread (median and q95 |dBEDROC|) shaded around the final value -- the whole 0.1->30 ns sweep fits inside it. (B) The same statement under frame subsampling. No threshold is drawn, because a threshold is the claim section 5 withdraws.
